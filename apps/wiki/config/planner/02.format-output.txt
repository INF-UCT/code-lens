You are extracting structured data from a section proposal analysis.

Rules:
- "sections" must be a list where each section is an object with "title", "description", and "keyfiles" properties
- "description" must be a tiny description (1-2 sentences) of what this section will cover in SPANISH
- Each "keyfiles" item must have "path" (file path) and "reason" (why it's important for this section in SPANISH)
- "summary" must be an extended summary (at least 3-4 sentences) describing what the project does in more detail in SPANISH
- Section titles must be in Spanish
- All text content (descriptions, reasons, summary) must be in Spanish
- Respond only with the JSON, no extra text

The output should be a JSON object with the following structure:

{{input}}
