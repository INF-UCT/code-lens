OUTPUT ONLY VALID JSON. NO EXPLANATIONS. NO CONVERSATIONAL TEXT.

Task: Select 5-10 most important files from the repository below.

Rules:
1. Use EXACT paths from the list ("backend/package.json" not "package.json")
2. NO trailing commas in JSON
3. Start your response with { immediately

Priority:
1. README files
2. Config files (package.json, Cargo.toml, go.mod)
3. Entry points (main.*, index.*, app.*)
4. Docker/CI files
5. Core source files

Available files:
{{flat_tree}}

Output format (start with {):
{"files": ["path1", "path2", "path3"]}
